<div class="wine-container">

    {%- assign collection = collections[section.settings.collection] -%}
    {% assign count = 0 %}
    <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
      <div class="carousel-inner">
        <ol class="indicators">
          {% for product in collection.products limit: 2%}
            <li data-current-bg="#{{product.metafields.theme.color}}" data-target="#carouselExampleIndicators" data-slide-to="{{count}}">
              <i class="fas fa-circle {% if count == 0 %} active {% endif %}"></i>
            </li>
          {% assign count = count | plus: 1 %}
          {% endfor %}
        </ol>
        <div class="controls">
            <a class="prev" href="#carouselExampleControls" role="button" data-slide="prev">
              <span class="fa fa-chevron-left" aria-hidden="true"></span>
              <span class="sr-only">Previous</span>
            </a>
            <a class="next" href="#carouselExampleControls" role="button" data-slide="next">
              <span class="fa fa-chevron-right" aria-hidden="true"></span>
              <span class="sr-only">Next</span>
            </a>
        </div>

        {% assign count = 0 %}
        {% for product in collection.products limit: 2%}
        <div id="wine{{count}}" data-current-bg="#{{product.metafields.theme.color}}" class="wine carousel-item {% if count == 0 %} active {% endif %}">
          {% include 'slide', current_product: product %}
        </div>
        {% assign count = count | plus: 1 %}
        {% endfor %}
      </div>
    </div>
</div>

{% schema %}
  {
    "name": "Slideshow collection",
    "class": "index-section",
    "settings": [
      {
        "id": "collection",
        "type": "collection",
        "label": "Collection"
      }
    ],
    "presets": [
      {
        "name": "Slideshow collection",
        "category": "Image"
      }
    ]
  }
{% endschema %}
